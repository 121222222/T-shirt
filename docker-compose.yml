version: '3.8'

services:
  tshirt-h5:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tshirt-h5
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
